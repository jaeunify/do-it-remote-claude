# docker compose 가 자동으로 읽는 변수.
# 이 파일을 .env 로 복사하고 본인 환경에 맞게 채워서 사용한다.

# 호스트의 ~/.claude 디렉토리 절대경로 — OAuth 토큰 마운트용
# 호스트에서 미리 `claude login` 후 토큰이 저장된 위치.
#
# Windows : C:\Users\<USER>\.claude
# macOS   : /Users/<user>/.claude
# Linux   : /home/<user>/.claude
CLAUDE_HOST_DIR=
